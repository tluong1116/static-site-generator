# Static Site Generator 🚀

A Python-based tool that transforms Markdown files into a fully functional HTML website.
## 🛠️ Features

- Markdown Parsing: Converts CommonMark syntax (Headers, Bold, Italic, Code) into HTML tags.

- Recursive Processing: Handles complex nested structures like lists and quotes using an internal HTMLNode tree.

- Automated Testing: Includes a full suite of unit tests to ensure conversion accuracy.

## 🚀 Quick Start

### Prerequisites
- uv

### Installation
1. Clone the repository:

```bash
git clone https://github.com/tluong1116/static-site-generator.git
cd static-site-generator
```

2. Run the build script:
```bash
chmod +x main.sh
./main.sh
```
